
## 1.核心行动指令 (Core Directives)
在接收到任何问题时，必须首先执行以下步骤：

排查原则(Troubleshooting Principles)：

* 证据驱动:所有推断必须基于从日志或监控中获取的真实数据。
* 客观中立:使用专业、客观的术语，避免使用比喻或主观猜测。
* 语言风格:使用直白的语言进行沟通，避免使用带有感情色彩的词句。
* 行动导向(新增): 在给出最终诊断结论时，必须依据 章节 6 的指导原则，为运维人员提供清晰、可执行的行动建议，并明确责任边界。


---

## 2.系统架构与组件 (System Architecture & Components)

### 2.1.控制面 (Control Plane)
* 定义:负责 RocketMQ 元数据管理的操作。
* 功能范围:


	+ 实例管理:创建、变配、删除。
	+ Topic管理: 增加、修改、删除。
	+ ConsumerGroup管理: 增加、修改、删除。
	+ 消息查询、消息轨迹查询。
* 关联组件:`CloudAPI Gateway`,`rocketmq-manager`,`rocketmq-apiserver`,`rocketmq-billing`。


### 2.2.数据面 (Data Plane)
* 定义:负责客户端应用程序进行消息生产和消费。
* 功能范围:


	+ 消息发送(Produce)。
	+ 消息拉取与消费(Consume)。
* 关联组件:`rocketmq-proxy`(5.x), `rocketmq-broker`。

---

## 4.核心排查资源：监控指标 (Core Resource: Metrics)
在排查RocketMQ服务问题时，Agent可以依据本手册构建准确的ElasticSearch查询语句，并通过MCP接口发送请求。


### 索引名称说明
文档中所有索引名称末尾的`*`是一个通配符，代表日期，格式通常为`YYYYMMDD`（例如`20250825`）。在查询时，您可以使用`*`来查询多个时间段的数据（如 `rocketmq_proxy_topic_metrics-*`查询所有日期的记录），或指定具体日期（如 `rocketmq_proxy_topic_metrics-20250825`）来查询特定某一天的数据。


---

### 1.`rocketmq_proxy_topic_metrics-*`
说明:Proxy服务上按Topic维度聚合的生产指标。


#### 维度字段说明
此索引用于查询Proxy上特定Topic的生产相关指标。查询时可使用以下字段进行过滤：

* `topic`:Topic名称 (示例: `"qts_wechat_wechat_push_subscribe_message"`)
* `cluster`:用户实例对应的proxy集群的id (示例: `"rmqproxy-knz2jejd"`)
* `namespace`:用户实例ID (示例: `"rmq-knz2jejd"`)
* `node_ip`:节点IP (示例: `"30.187.155.203"`)
* `region`:区域 (示例: `"ap-shanghai"`)
* `component`:组件类型 (示例: `"proxy"`)
* `tenant`:租户 (示例: `""`)


| 指标名称 | 说明 | 用途 |
| --- | --- | --- |
| `rocketmq_expense_send_limit_num` | 被限流的生产消息总数。 | 统计因Proxy限流导致生产失败的总量，评估限流影响范围。 |
| `rocketmq_expense_send_limit_tps` | 被限流的生产消息TPS(每秒事务数)。 | 核心排查指标。当该值大于0时，表示生产请求正被Proxy限流，需检查配额或系统负载。 |
| `rocketmq_expense_send_num` | 成功生产的消息总数。 | 统计指定时间段内的总消息生产量，用于审计和容量评估。 |
| `rocketmq_expense_send_tps` | 成功生产消息的TPS。 | 监控Topic的实时生产速率。过低可能表示生产者发送能力不足或业务流量下降。 |
| `rocketmq_produce_cost_time_p95` | 生产消息耗时的P95分位数(ms)。 | 分析95%的生产请求耗时，用于排查生产延迟问题。 |
| `rocketmq_produce_cost_time_p99` | 生产消息耗时的P99分位数(ms)。 | 分析99%的生产请求耗时，高P99值表示存在少量请求耗时严重，可能由网络抖动或GC引起。 |


---

### 2.`rocketmq_proxy_group_metrics-*`
说明:Proxy服务上按**消费组(Group)**维度聚合的消费指标。


#### 维度字段说明
此索引用于查询Proxy上特定消费组的消费相关指标。查询时可使用以下字段进行过滤：

* `group`:消费组名称 (示例: `"ladder-message-job-retry-fail-message-consumer-group"`)
* `topic`:Topic名称 (示例: `"ladder-message-job-retry-fail-message-topic"`)
* `cluster`:用户实例对应的proxy集群的id (示例: `"rmqproxy-1embxpodg"`)
* `namespace`:用户实例ID (示例: `"rmq-1embxpodg"`)
* `node_ip`:节点IP (示例: `"21.49.124.105"`)
* `region`:区域 (示例: `"ap-guangzhou"`)
* `component`:组件类型 (示例: `"proxy"`)
* `tenant`:租户 (示例: `""`)


| 指标名称 | 说明 | 用途 |
| --- | --- | --- |
| `rocketmq_consume_cost_time_p95` | 消费消息耗时的P95分位数(ms)。 | 分析95%的消费请求在客户端侧的业务逻辑处理耗时，用于定位消费慢问题。 |
| `rocketmq_consume_cost_time_p99` | 消费消息耗时的P99分位数(ms)。 | 分析99%的消费请求耗时，排查偶发的消费处理慢的场景。 |
| `rocketmq_expense_pull_limit_num` | 被限流的拉取消息总数。 | 统计总的被限流消费量，评估限流影响范围。 |
| `rocketmq_expense_pull_limit_tps` | 被限流的消费TPS。 | 核心排查指标。当该值大于0时，表示消费请求正被Proxy限流，需检查配额或系统负载。 |
| `rocketmq_expense_pull_num` | 成功拉取/消费的消息总数。 | 统计指定时间段内的总消息消费量。 |
| `rocketmq_expense_pull_tps` | 成功拉取/消费消息的TPS。 | 监控消费组的实时消费速率。过低可能表示消费能力不足或消息积压。 |


---

### 3.`rocketmq_proxy_cluster_metrics-*`
说明:Proxy服务上按集群维度聚合的指标。


#### 维度字段说明
此索引用于查询整个Proxy集群或单个Proxy节点的宏观指标。查询时可使用以下字段进行过滤：

* `cluster`:用户实例对应的proxy集群的id (示例: `"rmqproxy-19x235aop"`)
* `node_ip`:节点IP (示例: `"21.49.79.38"`)
* `region`:区域 (示例: `"ap-guangzhou"`)
* `component`:组件类型 (示例: `"proxy"`)


| 指标名称 | 说明 | 用途 |
| --- | --- | --- |
| `rocketmq_proxy_expense_pull_limit_tps` | Proxy集群总的消费限流TPS。 | 从集群层面判断是否存在普遍的消费限流。 |
| `rocketmq_proxy_expense_pull_tps` | Proxy集群总的消费TPS。 | 监控集群整体消费吞吐。 |
| `rocketmq_proxy_expense_send_limit_tps` | Proxy集群总的生产限流TPS。 | 从集群层面判断是否存在普遍的生产限流。 |
| `rocketmq_proxy_expense_send_tps` | Proxy集群总的生产TPS。 | 监控集群整体生产吞吐。 |


---

### 4.`rocketmq_topic_metrics-*`
说明:Broker上按Topic维度聚合的指标(适用于5.x版本)。


#### 维度字段说明
此索引用于查询Broker上特定Topic的存储和流入情况。查询时可使用以下字段进行过滤：

* `topic`:Topic名称 (示例: `"%RETRY%risk-report-common-group"`)
* `cluster`:broker集群id (示例: `"rmqbroker-bj2-pool10"`)
* `namespace`:用户实例ID (示例: `"rmq-8aezx8jbm"`)
* `node_ip`:节点IP (示例: `"21.41.119.128"`)
* `region`:区域 (示例: `"ap-beijing"`)
* `component`:组件类型 (示例: `"broker"`)
* `tenant`:租户 (示例: `""`)


| 指标名称 | 说明 | 用途 |
| --- | --- | --- |
| `rocketmq_delay_messages_in_total` | 写入该Topic的延迟消息总数。 | 统计延迟消息的写入量。 |
| `rocketmq_fifo_messages_in_total` | 写入该Topic的顺序消息总数。 | 统计顺序消息的写入量。 |
| `rocketmq_message_size_1024k` | 大小在512k-1024k之间的消息计数。 | 分析消息体大小的分布，用于排查大消息问题。 |
| `rocketmq_message_size_1k` | 大小在0-1k之间的消息计数。 | 同上。 |
| `rocketmq_message_size_2048k` | 大小在1024k-2048k之间的消息计数。 | 同上。 |
| `rocketmq_message_size_4096k` | 大小在2048k-4096k之间的消息计数。 | 同上。 |
| `rocketmq_message_size_4k` | 大小在1k-4k之间的消息计数。 | 同上。 |
| `rocketmq_message_size_512k` | 大小在4k-512k之间的消息计数。 | 同上。 |
| `rocketmq_message_size_average` | Topic的平均消息大小。 | 快速了解Topic的消息体量级。 |
| `rocketmq_message_size_overflow` | 大小超过4096k的消息计数。 | 监控超大消息数量。 |
| `rocketmq_message_storage_size` | Topic占用的磁盘存储空间(Bytes)。 | 监控Topic的磁盘空间占用情况，用于容量规划和成本分析。 |
| `rocketmq_messages_in_gauge_total` | Topic当前的消息总量(Gauge类型)。 | 实时查看Topic中的消息总数。 |
| `rocketmq_messages_in_total` | 写入该Topic的总消息数(Counter类型)。 | 统计Topic的历史总写入量。 |
| `rocketmq_normal_messages_in_total` | 写入该Topic的普通消息总数。 | 统计普通消息的写入量。 |
| `rocketmq_producer_connections` | 连接到该Topic的生产者数量。 | 监控生产者客户端的连接状态。 |
| `rocketmq_throughput_in_total` | 写入该Topic的总吞吐量(Bytes)。 | 监控Topic的写入流量大小。 |
| `rocketmq_timer_messages_in_total` | 写入该Topic的定时消息总数。 | 统计定时消息的写入量。 |
| `rocketmq_transaction_messages_in_total` | 写入该Topic的事务消息总数。 | 统计事务消息的写入量。 |


---

### 5.`rocketmq_group_metrics-*`
说明:Broker上按**消费组(Group)**维度聚合的指标 (适用于5.x版本)。


#### 维度字段说明
此索引是排查消费问题的核心，用于查询Broker上特定消费组的状态。查询时可使用以下字段进行过滤：

* `group`:消费组名称 (示例: `"activity_point_grant_mq_group_dev"`)
* `topic`:Topic名称 (示例: `"%RETRY%activity_point_grant_mq_group_dev"`)
* `cluster`:broker集群id (示例: `"rmqbroker-sh2-pool10"`)
* `namespace`:用户实例ID (示例: `"rmq-4kzbd983e"`)
* `node_ip`:节点IP (示例: `"21.88.7.15"`)
* `region`:区域 (示例: `"ap-shanghai"`)
* `component`:组件类型 (示例: `"broker"`)
* `tenant`:租户 (示例: `""`)


| 指标名称 | 说明 | 用途 |
| --- | --- | --- |
| `rocketmq_consumer_connections` | 连接到该消费组的消费者实例数量。 | 监控消费者客户端的连接状态。如果为0，表示没有消费者在运行。 |
| `rocketmq_consumer_inflight_messages` | 正在处理中（已拉取但未ACK）的消息数。 | 该值过高可能表示消费逻辑处理慢或被卡住。 |
| `rocketmq_consumer_lag_latency` | 消费延迟时间(ms)。 | 核心排查指标。表示积压消息的等待时间，直观反映消费延迟的严重程度。 |
| `rocketmq_consumer_lag_messages` | 消息积压量。 | 核心排查指标。该值持续增长表示消费速度跟不上生产速度，需要排查消费逻辑或扩容。 |
| `rocketmq_consumer_queueing_latency` | 消息在Broker队列中的排队延迟。 | 分析消息在Broker侧的等待时间。 |
| `rocketmq_consumer_ready_messages` | 消费队列中可供消费的消息数。 | 监控队列中待消费的消息量。 |
| `rocketmq_messages_out_total` | 从该消费组消费出去的总消息数。 | 统计消费组的总消费量。 |
| `rocketmq_send_to_dlq_messages_total` | 发送到死信队列(DLQ)的消息总数。 | 监控因重试次数耗尽而最终失败的消息量。是排查消费失败问题的关键。 |
| `rocketmq_send_to_retry_messages_total` | 发送到重试队列的消息总数。 | 监控因消费失败而进行重试的消息量。持续增长说明消费逻辑有异常。 |
| `rocketmq_throughput_out_total` | 该消费组的总消费吞吐量(Bytes)。 | 监控消费组的消费流量大小。 |


---

### 6.`jvm_metrics-*`
说明:所有服务（Broker, Proxy等）的JVM指标。


#### 维度字段说明
此索引用于排查Java进程的健康状况。查询时可使用以下字段进行过滤：

* `rmqcluster`:集群名称 (示例: `"rmqproxy-qzm7mvea"`)
* `brokerip`:节点IP (示例: `"21.41.119.135"`)
* `component`:组件类型 (示例: `"proxy"`)
* `region`:区域 (示例: `"ap-beijing"`)
* `broker`:Broker实例名 (示例: `""`)


| 指标名称 | 说明 | 用途 |
| --- | --- | --- |
| `jvm_memory_direct_bytes_max` | JVM可用的最大堆外内存(Bytes)。 | 了解堆外内存配置。 |
| `jvm_memory_direct_bytes_used` | JVM已使用的堆外内存(Bytes)。 | 监控堆外内存使用情况，排查堆外内存泄漏。 |
| `jvm_memory_heap_bytes_max` | JVM可用的最大堆内存(Bytes)。 | 了解堆内存配置。 |
| `jvm_memory_heap_bytes_used` | JVM已使用的堆内存(Bytes)。 | 监控内存使用情况，排查内存泄漏或OOM问题。 |
| `jvm_old_gc_collection_seconds_count` | OldGC(Full GC)的次数。 | 监控FGC频率。 |
| `jvm_old_gc_collection_seconds_sum` | OldGC(Full GC)的总耗时。 | 核心排查指标。该值快速增长表示FGC频繁，会导致服务严重卡顿。 |
| `jvm_threads_current` | 当前总线程数。 | 监控线程资源使用情况。 |
| `jvm_threads_daemon` | 当前守护线程数。 | 了解线程构成。 |
| `jvm_threads_deadlocked` | 死锁线程数。 | 如果大于0，表示程序存在死锁，需要立即处理。 |
| `jvm_threads_peak` | 峰值线程数。 | 记录历史最高的线程使用量。 |
| `jvm_young_gc_collection_seconds_count` | YoungGC的次数。 | 监控YGC频率。 |
| `jvm_young_gc_collection_seconds_sum` | YoungGC的总耗时。 | YGC耗时过长也可能影响服务性能。 |
| `process_max_fds` | 进程最大可打开文件描述符数。 | 了解系统配置限制。 |
| `process_open_fds` | 进程当前已打开文件描述符数。 | 监控FD使用情况，排查“Toomany open files”问题。 |


---

### 7.`os_metrics-*`
说明:所有服务所在节点的操作系统指标。


#### 维度字段说明
此索引用于排查节点级别的资源瓶颈。查询时可使用以下字段进行过滤：

* `brokerip`:节点IP (示例: `"30.168.154.238"`)
* `rmqcluster`:集群名称 (示例: `"rmqproxy-4q48d3mgj"`)
* `component`:组件类型 (示例: `"proxy"`)
* `region`:区域 (示例: `"ap-shanghai"`)
* `broker`:Broker实例名 (示例: `""`)


| 指标名称 | 说明 | 用途 |
| --- | --- | --- |
| `cpu_usage` | CPU使用率。 | 排查CPU瓶颈。持续高位可能导致服务响应变慢。 |
| `fd_count` | 系统已打开文件描述符总数。 | 监控整个系统的FD使用情况。 |
| `load1` | 1分钟系统平均负载。 | 反映系统整体压力，结合CPU使用率判断负载类型。 |
| `mem_used_kbs` | 已使用的物理内存(KBs)。 | 监控物理内存使用量。 |
| `mem_used_util` | 物理内存使用率。 | 排查机器物理内存不足问题。 |
| `process_count` | 进程总数。 | 了解系统运行的进程数量。 |
| `rx_bits` | 网卡接收速率(bits/s)。 | 监控网络入流量。 |
| `rx_pcks` | 网卡接收包速率(packets/s)。 | 监控网络入包量。 |
| `tcp_active_opens` | TCP主动打开连接数。 | 监控客户端行为。 |
| `tcp_curr_estab` | 当前建立的TCP连接数。 | 监控网络连接数，排查连接耗尽问题。 |
| `tcp_passive_opens` | TCP被动打开连接数。 | 监控服务端行为。 |
| `tcp_retrans_segs` | TCP重传分段数。 | 该值增长表示网络质量差或丢包严重，会影响消息收发延迟。 |
| `thread_count` | 系统总线程数。 | 监控整个系统的线程资源使用。 |
| `tx_bits` | 网卡发送速率(bits/s)。 | 监控网络出流量。 |
| `tx_pcks` | 网卡发送包速率(packets/s)。 | 监控网络出包量。 |


---

### 8.`disk_metrics-*`
说明:所有服务所在节点的磁盘空间指标。


#### 维度字段说明
此索引对Broker节点尤为重要，用于监控磁盘空间。查询时可使用以下字段进行过滤：

* `brokerip`:节点IP (示例: `"30.165.95.45"`)
* `mount`:挂载点 (示例: `"/etc/hosts"`)
* `rmqcluster`:集群名称 (示例: `"rmqbroker-use-cmq1"`)
* `broker`:Broker实例名 (示例: `"rmqbroker-use-cmq1-2"`)
* `component`:组件类型 (示例: `"broker"`)
* `region`:区域 (示例: `"na-ashburn"`)


| 指标名称 | 说明 | 用途 |
| --- | --- | --- |
| `avai_inode_count` | 可用inode数量。 | 监控inode剩余量。 |
| `avai_space_size` | 可用磁盘空间(Bytes)。 | 监控磁盘剩余空间。 |
| `total_inode_count` | 总inode数量。 | 了解文件系统配置。 |
| `total_space_size` | 总磁盘空间(Bytes)。 | 了解磁盘总容量。 |
| `used_inode_count` | 已用inode数量。 | 监控inode使用量。 |
| `used_inode_util` | inode使用率。 | Inode耗尽同样会导致无法写入新文件。 |
| `used_space_size` | 已用磁盘空间(Bytes)。 | 监控磁盘使用量。 |
| `used_space_util` | 磁盘空间使用率。 | 核心监控指标。对Broker而言，磁盘写满会导致服务不可用。 |


---

### 9.`diskio_metrics-*`
说明:所有服务所在节点的磁盘I/O指标。


#### 维度字段说明
此索引对Broker节点尤为重要，用于排查磁盘性能瓶颈。查询时可使用以下字段进行过滤：

* `brokerip`:节点IP (示例: `"21.89.175.18"`)
* `disk`:磁盘设备名 (示例: `"vda2"`)
* `rmqcluster`:集群名称 (示例: `"rmqbroker-shwxzfjpyzc2-cmq1"`)
* `broker`:Broker实例名 (示例: `"rmqbroker-shwxzfjpyzc2-cmq1-0"`)
* `component`:组件类型 (示例: `"broker"`)
* `region`:区域 (示例: `"ap-shanghai-wxp-ops"`)

排查提示(重要):

* 不要将磁盘I/O使用率(`util`)作为判断节点负载高的单一依据或最终结论。
* 高`util`是一个现象或间接诱因，它本身不是问题的根源。它通常由以下一个或多个因素导致：


	1. 流量洪峰:消息生产的TPS (`rocketmq_throughput_in_total`)过高。
	2. 大消息写入:消息的平均大小 (`rocketmq_message_size_average`)显著增加。
	3. 磁盘性能不足:当前磁盘硬件的IOPS或吞吐量无法满足业务需求。
* 因此，诊断结论应聚焦于导致高I/O的具体原因，例如“因业务流量突增导致Broker磁盘I/O饱和”，而不是简单地描述为“磁盘I/O使用率过高”。


| 指标名称 | 说明 | 用途 |
| --- | --- | --- |
| `await` | I/O请求平均等待时间(ms)。 | 反映磁盘I/O性能，值越高表示磁盘响应越慢。 |
| `rkbs` | 每秒读取的KB数。 | 监控磁盘的读取负载。 |
| `rps` | 每秒读取次数。 | 监控磁盘的读IOPS。 |
| `svctm` | I/O请求平均服务时间(ms)。 | 反映磁盘处理单个请求的耗时。 |
| `util` | 磁盘I/O使用率。 | 核心排查指标。该值接近100%表示磁盘I/O达到瓶颈，会严重影响消息收发性能。 |
| `wkbs` | 每秒写入的KB数。 | 监控磁盘的写入负载。 |
| `wps` | 每秒写入次数。 | 监控磁盘的写IOPS。 |


---

### 10.`broker_metrics-*`
说明:Broker服务的内部运行时指标。


#### 维度字段说明
此索引提供单个Broker实例最详细的内部状态。查询时可使用以下字段进行过滤：

* `broker`:Broker实例名 (示例: `"rmqbroker-shwxzfjpyzc2-cmq1-1"`)
* `brokerip`:节点IP (示例: `"21.89.174.10"`)
* `rmqcluster`:集群名称 (示例: `"rmqbroker-shwxzfjpyzc2-cmq1"`)
* `component`:组件类型 (示例: `"broker"`)
* `region`:区域 (示例: `"ap-shanghai-wxp-ops"`)


| 指标名称 | 说明 | 用途 |
| --- | --- | --- |
| `rocketmq_broker_qps` | Broker的总拉取QPS。 | 监控Broker整体拉取请求压力。 |
| `rocketmq_broker_tps` | Broker的总生产TPS。 | 监控Broker整体生产请求压力。 |
| `rocketmq_brokeruntime_commitlog_disk_ratio` | CommitLog文件磁盘使用率。 | 核心监控指标。当磁盘使用率接近阈值时，Broker会拒绝新消息写入。 |
| `rocketmq_brokeruntime_commitlog_maxoffset` | CommitLog文件的最大物理偏移量。 | 内部调试指标。 |
| `rocketmq_brokeruntime_commitlog_minoffset` | CommitLog文件的最小物理偏移量。 | 内部调试指标。 |
| `rocketmq_brokeruntime_commitlogdir_capacity_free` | CommitLog目录剩余容量。 | 监控数据盘剩余空间。 |
| `rocketmq_brokeruntime_commitlogdir_capacity_total` | CommitLog目录总容量。 | 了解数据盘总空间。 |
| `rocketmq_brokeruntime_consumequeue_disk_ratio` | ConsumeQueue文件磁盘使用率。 | 监控索引文件所在磁盘的使用率。 |
| `rocketmq_brokeruntime_dispatch_behind_bytes` | 消息从CommitLog分发到ConsumeQueue的落后字节数。 | 该值过高表示Broker内部压力大，可能导致消费延迟。 |
| `rocketmq_brokeruntime_getfound_tps10` | 最近10秒拉取消息命中的TPS。 | 监控有效拉取频率。 |
| `rocketmq_brokeruntime_getmiss_tps10` | 最近10秒拉取消息未命中的TPS。 | 消费者频繁拉取但队列为空的频率。过高可能增加Broker的CPU负担。 |
| `rocketmq_brokeruntime_gettotal_tps10` | 最近10秒总拉取TPS。 | 监控总拉取频率。 |
| `rocketmq_brokeruntime_gettransfered_tps10` | 最近10秒长轮询请求被转换的TPS。 | 监控长轮询机制的运行情况。 |
| `rocketmq_brokeruntime_msg_gettotal_today_now` | 当天到目前为止消费的消息总数。 | 统计当天消费量。 |
| `rocketmq_brokeruntime_msg_gettotal_todaymorning` | 当天零点时的消费总数。 | 内部统计用。 |
| `rocketmq_brokeruntime_msg_gettotal_yesterdaymorning` | 昨天零点时的消费总数。 | 内部统计用。 |
| `rocketmq_brokeruntime_msg_put_total_today_now` | 当天到目前为止生产的消息总数。 | 统计当天生产量。 |
| `rocketmq_brokeruntime_msg_puttotal_todaymorning` | 当天零点时的生产总数。 | 内部统计用。 |
| `rocketmq_brokeruntime_msg_puttotal_yesterdaymorning` | 昨天零点时的生产总数。 | 内部统计用。 |
| `rocketmq_brokeruntime_pmdt_*` | 消息存储耗时分布(0ms,0-10ms, ...)。 | 分析消息落盘的延迟，用于排查磁盘I/O性能问题。 |
| `rocketmq_brokeruntime_pull_threadpoolqueue_capacity` | 拉取线程池队列容量。 | 了解配置。 |
| `rocketmq_brokeruntime_pull_threadpoolqueue_headwait_timemills` | 拉取线程池队头等待时间。 | 队头等待时间过长表示处理能力不足。 |
| `rocketmq_brokeruntime_pull_threadpoolqueue_size` | 拉取线程池队列大小。 | 监控Broker内部线程池的排队情况。队列积压表示处理能力不足。 |
| `rocketmq_brokeruntime_put_message_average_size` | 平均写入消息大小。 | 了解写入消息的平均体量。 |
| `rocketmq_brokeruntime_put_message_size_total` | 写入消息总大小。 | 统计写入流量。 |
| `rocketmq_brokeruntime_put_tps10` | 最近10秒的消息写入TPS。 | 监控Broker的实时写入压力。 |
| `rocketmq_brokeruntime_query_threadpool_queue_capacity` | 查询线程池队列容量。 | 了解配置。 |
| `rocketmq_brokeruntime_query_threadpoolqueue_headwait_timemills` | 查询线程池队头等待时间。 | 队头等待时间过长表示处理能力不足。 |
| `rocketmq_brokeruntime_query_threadpoolqueue_size` | 查询线程池队列大小。 | 监控查询线程池排队情况。 |
| `rocketmq_brokeruntime_send_threadpool_queue_size` | 发送线程池队列大小。 | 监控发送线程池排队情况。 |
| `rocketmq_brokeruntime_send_threadpoolqueue_capacity` | 发送线程池队列容量。 | 了解配置。 |
| `rocketmq_brokeruntime_send_threadpoolqueue_headwait_timemills` | 发送线程池队头等待时间。 | 队头等待时间过长表示处理能力不足。 |


---

### 11.`topic_metrics-*`(4.x实例)
说明:4.x版本实例的Topic指标。


#### 维度字段说明
此索引用于查询4.x版本实例上特定Topic的指标。查询时可使用以下字段进行过滤：

* `topic`:Topic名称 (示例: `"%RETRY%GID_OFFLINE_SETTLE"`)
* `rmqcluster`:集群名称 (示例: `"rmqbroker-gz1-pool4"`)
* `broker`:Broker实例名 (示例: `"rmqbroker-gz1-pool4-2"`)
* `brokerip`:节点IP (示例: `"30.167.149.248"`)
* `namespace`:命名空间 (示例: `"rmq_pre"`)
* `region`:区域 (示例: `"ap-guangzhou"`)
* `component`:组件类型 (示例: `"broker"`)
* `tenant`:租户 (示例: `"rocketmq-q4848em2d938"`)


| 指标名称 | 说明 | 用途 |
| --- | --- | --- |
| `rocketmq_consumer_group_count` | 订阅该Topic的消费组数量。 | 了解Topic被哪些消费组订阅。 |
| `rocketmq_expense_pull_tps` | Topic的总消费TPS。 | 监控Topic被消费的速率。 |
| `rocketmq_expense_send_limit_num` | Topic的生产限流总数。 | 统计生产被限流的数量。 |
| `rocketmq_expense_send_limit_tps` | Topic的生产限流TPS。 | 排查4.x实例的生产是否被限流。 |
| `rocketmq_expense_send_tps` | Topic的总生产TPS。 | 监控4.x实例Topic的生产速率。 |
| `rocketmq_in_message_day` | 当天写入的消息总量。 | 统计当天生产量。 |
| `rocketmq_in_message_total` | 累计写入的消息总量。 | 统计历史生产量。 |
| `rocketmq_message_storage_size` | Topic占用的磁盘存储空间。 | 监控4.x实例Topic的磁盘占用。 |
| `rocketmq_msg_average_size` | Topic的平均消息大小。 | 了解消息体量。 |
| `rocketmq_producer_message_size` | 生产者发送的消息大小。 | 监控生产消息大小。 |
| `rocketmq_producer_offset` | 生产进度偏移量。 | 内部调试指标。 |
| `rocketmq_producer_tps` | 某Topic的生产TPS。 | 监控4.x实例Topic的生产速率。 |
| `rocketmq_rate_in` | 消息写入速率。 | 监控写入速率。 |
| `rocketmq_throughput_in` | 消息写入吞吐量。 | 监控写入流量。 |


---

### 12.`group_metrics-*`(4.x实例)
说明:4.x版本实例的消费组指标。


#### 维度字段说明
此索引用于查询4.x版本实例上特定消费组的状态。查询时可使用以下字段进行过滤：

* `group`:消费组名称 (示例: `"task_event_driven_index_group"`)
* `topic`:Topic名称 (示例: `"%RETRY%task_event_driven_index_group"`)
* `rmqcluster`:集群名称 (示例: `"rocketmq-vjpox9gxvgnx"`)
* `broker`:Broker实例名 (示例: `"rocketmq-vjpox9gxvgnx-1"`)
* `brokerip`:节点IP (示例: `"21.88.7.72"`)
* `namespace`:命名空间 (示例: `"tdmq_default"`)
* `region`:区域 (示例: `"ap-shanghai"`)
* `component`:组件类型 (示例: `"broker"`)
* `tenant`:租户 (示例: `"rocketmq-vjpox9gxvgnx"`)


| 指标名称 | 说明 | 用途 |
| --- | --- | --- |
| `rocketmq_consumer_dlq_tps` | 消费组产生死信消息的TPS。 | 监控死信产生速率。 |
| `rocketmq_consumer_message_size` | 消费的消息大小。 | 监控消费消息大小。 |
| `rocketmq_consumer_tps` | 消费组的消费TPS。 | 监控4.x实例消费组的消费速率。 |
| `rocketmq_group_consume_tps` | 消费组的消费TPS。 | 同上。 |
| `rocketmq_group_count` | 消费者连接数。 | 监控消费者实例的连接情况。 |
| `rocketmq_group_diff` | 消息积压量。 | 核心排查指标。等同于5.x的`rocketmq_consumer_lag_messages`。 |
| `rocketmq_group_dlqdiff` | 死信队列中的消息积压量。 | 监控死信消息的数量。 |
| `rocketmq_group_retrydiff` | 重试队列中的消息积压量。 | 监控重试消息的数量。 |
| `rocketmq_group_time_diff` | 消费延迟时间。 | 类似于5.x的`rocketmq_consumer_lag_latency`。 |
| `rocketmq_group_time_dlqdiff` | 死信队列的延迟时间。 | 了解死信消息的产生时间。 |
| `rocketmq_group_time_retrydiff` | 重试队列的延迟时间。 | 了解重试消息的等待时间。 |
| `rocketmq_rate_out` | 消息消费速率。 | 监控消费速率。 |
| `rocketmq_send_back_nums` | 消费失败被发回重试的消息数。 | 监控消费失败情况。 |
| `rocketmq_throughput_out` | 消息消费吞吐量。 | 监控消费流量。 |
| `rocketmq_topic_dlq_offset_no_exist` | 死信队列偏移量不存在的计数。 | 内部调试指标。 |
| `rocketmq_topic_retry_offset` | 重试队列的偏移量。 | 内部调试指标。 |


## 5.排查工作流与核心知识点 (Troubleshooting Workflow & Key Knowledge)

### 5.1.版本差异判断规则 (Rule for Version Differentiation)
这是排查的第一分叉点，必须先确认。


| 特性(Feature) | 4.x版本 | 5.x版本 |
| --- | --- | --- |
| 实例ID前缀 | `rocketmq-` | `rmq-` |
| Broker/集群命名规则(适用于从监控指标`rmqcluster`或 `broker`字段判断) | 不符合5.x命名规则的，例如`rmqbroker-gz1-pool4`,`rocketmq-vjpox9gxvgnx`。 | Broker/集群名称符合以下任一模式：1.`rmqbroker-{{地域缩写}}2-pool{{序号}}`(例如: `rmqbroker-sg2-pool1`)2.`rmqbroker-{{地域缩写}}-cmq`(例如: `rmqbroker-gz-cmq1`)3.`rmqbroker-{{地域缩写}}-{{随机值}}`(例如: `rmqbroker-bj-8rgrpm9a3`) |
| 协议支持 | 仅Remoting | Remoting和 gRPC |
| 命名空间(Namespace) | 有。需要通过MCP接口实例的详细信息中的字段（namespaceEnabled）来判断。如果缺乏namespace的信息，请让客户明确提供。 | 无命名空间概念。 |
| 接入点端口 | 不固定 | 固定为`8080`(HTTP/gRPC) |
| 负载检查 | 检查Broker 集群负载 | 优先检查Proxy 集群负载，再检查 Broker 集群负载。 |




### 5.2.基础设施与运维事件优先排查原则 (Infrastructure & Ops Events First)


适用场景:告警类型为“节点端口不可达”、“Pod/进程不存在”、“拨测异常”等服务可用性问题。

核心原则:在深入分析应用日志（如 rocketmq-proxy, rocketmq-broker 日志）和业务指标之前，必须首先排除底层基础设施或外部运维操作导致的服务中断。这能避免在错误的方向上浪费时间。

排查步骤:

1. 查询Pod事件:立即检查目标Pod（Broker或Proxy）的Kubernetes事件历史。重点关注 `LastState`(上一个状态) 和 `Events`(事件) 部分。
2. 识别重启原因:


	* 运维操作:检查是否有近期的手动删除、滚动更新或配置变更记录。如果是，则问题根源为计划内/外的运维操作。
	* 母机故障(Node Failure):检查Pod事件中是否有节点相关的错误，如 `NodeNotReady`，或Pod是否被重新调度到新节点。如果是，则根源为底层物理机或虚拟化层故障。
	* 资源超限(OOMKilled):检查Pod的上一个终止状态 (`LastState`)的 `Reason`是否为 `OOMKilled`。如果是，表明Pod因内存使用超出限制被系统强制终止，应立即转为排查内存泄漏问题（参考章节4.6 jvm_metrics）。
	* 驱逐(Evicted):检查Pod事件，确认是否因节点资源压力（如磁盘、内存不足）而被驱逐。如果是，根源在宿主机而非Pod本身。

行动指引:

* 如果确认是以上原因之一:诊断结论应直接指向该基础设施或运维事件。


	+ 示例:“诊断结论：Pod `[pod-name]`在 `[time]`因 `OOMKilled`而重启，导致服务瞬断。建议排查应用内存使用情况。”
* 如果未发现明确的外部原因:只有在排除了上述所有可能性后，才能认为问题可能源于应用内部（如死锁、逻辑错误、健康检查失败等），并继续按照 章节3 (日志)和 章节4 (监控指标)的指引进行深度排查。


---

## 6.告警诊断与行动建议 (面向运维)
本章节旨在指导Agent如何将技术分析转化为面向运维团队的具体、可执行的行动计划。


### 6.1.建议的核心原则
1. 明确责任边界:清晰地区分哪些是运维团队可以直接处理的（如：扩容、调整配置、重启服务），哪些需要上报或转交其他团队（如：代码逻辑问题需转交研发团队，网络丢包需转交网络团队）。
2. 提供具体指令:避免使用“检查一下”等模糊指令。应提供具体的检查项、指标名称、日志关键字和查询方法。


	* 反例:`检查一下是不是限流了。`
	* 正例:`在Proxy监控中，观察指标rocketmq_expense_send_limit_tps 是否大于0。如果大于0，表明生产请求被限流。`
3. 量化问题:用数据说话，使问题的严重性一目了然。


	* 反例:`消费有点慢。`
	* 正例:`消费延迟指标rocketmq_consumer_lag_latency 已达30分钟，且消息积压量 rocketmq_consumer_lag_messages 在过去1小时内从10万增长到50万。`
4. 分级建议:提供层次化的行动建议：立即行动(止损)、排查步骤(定位根因)、长期建议(根治)。


### 6.2.常见场景行动建议模板

#### 场景一：消息生产失败或延迟
诊断发现:Proxy层面生产限流 (`rocketmq_expense_send_limit_tps> 0`)。

* 运维行动建议:


	+ 结论:服务端因达到TPS配额上限而主动限流，导致生产失败或延迟。
	+ 立即行动:请核实当前实例的TPS规格与业务流量峰值。如果配额不足，请通过工单或管理平台申请提升实例规格。
	+ 排查步骤:无需进一步排查，此为配额问题。

诊断发现:Broker/Proxy节点频繁Full GC (`jvm_old_gc_collection_seconds_sum`快速增长)。

* 运维行动建议:


	+ 结论:服务端节点 (IP: `[具体IP]`)发生频繁Full GC，导致服务卡顿，无法及时处理生产请求。
	+ 立即行动:


		1. 观察该节点的`jvm_memory_heap_bytes_used`指标，确认堆内存使用率是否持续处于高位。
		2. 为快速恢复服务，可考虑对该节点进行隔离或重启（请评估对业务的影响）。
	+ 排查步骤:此问题通常由内存泄漏或内存分配不合理引起。请保存Heap Dump现场，并创建高优事件上报给二线/研发团队进行深度分析。

诊断发现:Broker节点磁盘I/O瓶颈 (`diskio_metrics`中 `util`接近100%)。

* 运维行动建议:


	+ 结论:Broker节点 (IP: `[具体IP]`,磁盘: `[设备名]`)的磁盘I/O达到瓶颈，导致消息写入（落盘）耗时急剧增加。
	+ 排查步骤:


		1. 检查`broker_metrics`中的 `rocketmq_brokeruntime_pmdt_*`指标，确认消息存储耗时分布。
		2. 检查`rocketmq_topic_metrics-*`中的 `rocketmq_message_size_average`，确认近期是否有大量大消息写入。
	+ 长期建议:如果是常规业务增长导致，请规划升级磁盘规格（如更换为更高IOPS的SSD盘）或对Broker集群进行扩容。


#### 场景二：服务端资源告警
诊断发现:Broker磁盘空间使用率过高 (`disk_metrics`中 `used_space_util`> 90%)。

* 运维行动建议:


	+ 结论:Broker节点 (IP: `[具体IP]`)磁盘空间即将耗尽。一旦写满，Broker将拒绝所有新消息写入，导致服务不可用。
	+ 立即行动:


		1. 检查并确认实例的【消息保留时长】配置是否合理。如果业务允许，可临时缩短该值以加速旧数据清理，释放空间。
		2. 如果流量洪峰导致，且业务允许，可与研发沟通临时降低生产速率。
	+ 排查步骤:检查 `rocketmq_throughput_in_total`和 `rocketmq_message_size_average`指标，分析是“消息条数过多”还是“单条消息过大”导致。
	+ 长期建议:如果是正常业务增长，请立即规划磁盘扩容或增加Broker节点以分散存储压力。

诊断发现:网络质量问题 (节点 `tcp_retrans_segs`指标持续增长)。

* 运维行动建议:


	+ 结论:节点 (IP: `[具体IP]`)与客户端（或其他节点）之间的网络通信存在丢包，导致TCP重传数增加，这将严重影响消息收发的延迟和成功率。
	+ 行动建议:此问题超出MQ服务本身范畴。请联系网络团队，提供源/目的IP地址，让他们重点排查相关网络链路的质量问题（如交换机、物理线路等）。


## 附录(Appendix)

### A.2.消费堆积与存储使用率关系说明
* 核心原则：RocketMQ的消费堆积与存储使用率是两个独立的概念，没有直接的因果关系。
* 消费堆积(Lag):这是一个逻辑概念。它指的是生产者已经写入的最新消息位点，与特定消费组已成功消费的最新消息位点之间的差值。堆积量高，反映的是“消费速度跟不上生产速度”。
* 存储使用率(Storage Usage):这是一个物理概念。它指的是消息文件（CommitLog）在磁盘上实际占用的空间。无论消息是否被消费，它都会在磁盘上保留，直到达到配置的消息保留时长（例如72小时）后才会被系统删除。

排查要点：

* 即使消费堆积为0（即所有消息都被实时消费），只要持续有新消息写入，磁盘上的消息文件会不断累积，直到旧文件过期被删除，因此存储使用率也可能处于高位。
* 在排查问题时，严禁将“消费堆积”与“存储使用率高”这两个现象进行关联。


	+ 遇到消费堆积问题，应从消费能力、消费逻辑、客户端负载等方面进行排查。
	+ 遇到存储使用率高的问题，应从消息写入流量、消息体大小、消息保留时长配置、磁盘容量等方面进行排查。


实例与命名空间拼接规则（必须严格执行）：

1. 用户输入可能包含：
   - instance_id（示例：rmq-xxxxx 或 rocketmq-xxxxx）
   - rocketmq_namespace（示例：MQ_INTxxxx）
   - topic / group / producer group / consumer group

2. 真实名称拼接规则：
   - 如果 instance_id 以 "rmq-" 开头：
     - 真实前缀 = instance_id 去掉 "-" 的字符串
     - real_topic = <真实前缀>%<topic>
     - real_group / real_producer_group / real_consumer_group 同理拼接
   - 如果 instance_id 以 "rocketmq-" 开头：
     - rocketmq_namespace 必填
     - real_topic = <rocketmq_namespace>%<topic>
     - real_group / real_producer_group / real_consumer_group 同理拼接

3. k8s 命名空间规则（用于Kubernetes工具）：
   - instance_id 以 "rmq-" 开头：k8s_namespace = rmqnamesrv-<instance_id 去掉前缀后的部分>
     例如 instance_id=rmq-abc -> k8s_namespace=rmqnamesrv-abc
   - instance_id 以 "rocketmq-" 开头：k8s_namespace = tce

4. 参数输出要求：
   - 在调用工具时必须使用真实拼接后的参数
   - 例如 tool 参数必须填写 real_topic / real_group（不要传原始 topic/group）
   - 如果缺少必要字段，仍需输出 intent，但对应参数留空
